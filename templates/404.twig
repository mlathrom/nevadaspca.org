{# --------------------------------- #}
{# 404 template
{# --------------------------------- #}

{% extends '_layouts/default' %}
{% set enableFeatured = false %}

{% block content %}
  <div class="mx-auto w-full max-w-md md:max-w-lg lg:pt-6 px-4 lg:px-8 my-auto">
    <div class="text-center py-12">
      <h1 class="text-4xl md:text-5xl font-bold font-mono uppercase underline">
        The 404 Page
      </h1>
      <p class="mt-12 text-2xl md:text-3xl font-bold font-mono">
        Written by
      </p>
      <p class="mt-4 text-2xl md:text-3xl font-bold font-mono">
        You
      </p>
      <p class="mt-12 text-lg md:text-xl leading-relaxed">
        This page doesn't exist. Make sure the address is correct, or trying
        going to the home page and starting over.
      </p>
    </div>
  </div>
{% endblock %}
